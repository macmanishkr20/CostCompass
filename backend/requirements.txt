# CostCompass backend — FastAPI + LangGraph deterministic estimation engine
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
langgraph>=0.2
anthropic>=0.40
reportlab>=4.2
openpyxl>=3.1
httpx>=0.27

# Optional — only needed when COSMOS_ENDPOINT/COSMOS_KEY are configured.
# Persistence falls back to local JSON files when this isn't installed.
# azure-cosmos>=4.7
